---
interface Props {
  dataBase: string;
}

const { dataBase } = Astro.props;
---

<div class="film-atlas-page relative left-1/2 w-screen -translate-x-1/2 overflow-hidden bg-[#050505] text-[#f7ead2]" data-film-atlas-root data-data-base={dataBase}>
  <section class="atlas-shell relative overflow-hidden border-b border-[#f2c46d]/15">
    <div class="atlas-page-field absolute inset-0"></div>
    <div class="atlas-app-frame relative mx-auto flex max-w-[1540px] flex-col">
      <nav class="atlas-site-nav" aria-label="Project navigation">
        <a class="atlas-site-home" href="/">The Film Atlas</a>
        <div class="atlas-site-links" aria-label="Project sections">
          <a href="#atlas-map">Map</a>
          <a href="#atlas-method">Method</a>
          <a href="#atlas-writeup">Build</a>
        </div>
      </nav>

      <header class="atlas-masthead">
        <div class="atlas-title-block">
          <p class="atlas-eyebrow">Film Microgenres Mapped</p>
          <h1>The Film Atlas</h1>
          <p class="atlas-dek">
            An interactive map of 10,000 of the most popular films on TMDb, released from 1980 onward and arranged by experiential similarity: story, mood, texture, theme, and audience perception, not genre alone.
          </p>
        </div>
        <dl class="atlas-stat-grid">
          <div>
            <dt>Movies</dt>
            <dd data-atlas-movie-count>10,000</dd>
          </div>
          <div>
            <dt>Built</dt>
            <dd data-atlas-generated-at>June 2026</dd>
          </div>
        </dl>
      </header>

      <div class="atlas-workspace">
        <section id="atlas-map" class="atlas-map-panel" aria-label="Interactive movie map">
          <div class="atlas-toolbar">
            <form class="atlas-search-form" data-atlas-search-form>
              <input
                id="film-atlas-search"
                class="atlas-search-input"
                data-atlas-search
                aria-label="Search by movie title"
                autocomplete="off"
                placeholder="Search by movie title"
                type="search"
              />
              <div class="atlas-search-results" data-atlas-search-results hidden></div>
            </form>

            <div class="atlas-control-rack">
              <span class="atlas-tier-pill" data-atlas-label-tier>Macro regions</span>
              <div class="atlas-zoom-controls">
                <button class="atlas-icon-button" type="button" data-atlas-zoom-out aria-label="Zoom out">-</button>
                <button class="atlas-icon-button" type="button" data-atlas-zoom-in aria-label="Zoom in">+</button>
                <button class="atlas-reset-button" type="button" data-atlas-reset>Reset</button>
              </div>
            </div>
          </div>

          <div class="atlas-stage" data-atlas-stage>
            <canvas class="absolute inset-0 h-full w-full touch-none cursor-grab" data-atlas-canvas aria-label="Interactive semantic territory map of 10,000 films"></canvas>
            <div class="atlas-tooltip" data-atlas-tooltip hidden></div>
            <div class="atlas-loading" data-atlas-status hidden>
              Loading constellation
            </div>
            <button class="atlas-selected-map-chip" type="button" data-atlas-selected-chip hidden>
              <span data-atlas-selected-chip-title></span>
              <span class="atlas-selected-map-chip-action">Recenter</span>
            </button>
          </div>
        </section>

        <aside class="atlas-detail-panel">
          <div class="atlas-detail-scroll">
            <p class="atlas-panel-eyebrow" data-atlas-panel-eyebrow>Start exploring</p>
            <h2 data-atlas-selected-title>
              Choose a film
            </h2>
            <p class="atlas-selected-meta" data-atlas-selected-meta>
              Search or click any dot
            </p>
            <div class="atlas-genre-row" data-atlas-selected-genres></div>
            <p class="atlas-selected-overview" data-atlas-selected-overview>
              Search for a movie or click any dot on the map to see where it lives in the atlas. Each film belongs to a nested semantic path: a broad region, a local neighborhood, and a tight micro cluster.
            </p>

            <div class="atlas-selected-labels" data-atlas-selected-labels></div>

            <div class="atlas-neighbor-section" data-atlas-neighbor-section hidden>
              <h3>Nearest neighbors</h3>
              <div class="atlas-neighbor-list" data-atlas-neighbors></div>
            </div>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <section id="atlas-method" class="atlas-method-section">
    <div class="atlas-method-grid">
      <div class="atlas-method-heading">
        <p class="atlas-eyebrow">How to read it</p>
        <h2>A map of movie meaning, not just movie categories.</h2>
      </div>
      <div class="atlas-method-copy">
        <p>
          Every dot is one film. The atlas maps 10,000 popular TMDb movies released from 1980 onward. At the widest zoom, it shows broad cinematic territories: crime worlds, family animation, dystopian futures, romance and becoming, survival dread, mythic spectacle, and more.
        </p>
        <p>
          Zoom in and those territories split into neighborhoods. Zoom further and neighborhoods reveal micro clusters: smaller groups of films with unusually similar themes, tone, story mechanics, or audience vibe.
        </p>
        <p>
          Search for a film, click a dot, or follow a nearest neighbor to move through the map. The side panel shows the selected movie's macro region, neighborhood, micro cluster, and closest semantic neighbors.
        </p>
        <p>
          Visual distance is a guide, not the whole truth. Nearest neighbors are calculated in the full semantic embedding space; the map is a browser-rendered projection designed to make that high-dimensional structure explorable.
        </p>
      </div>
    </div>
  </section>

  <section class="atlas-method-section atlas-method-section--inspiration">
    <div class="atlas-method-grid">
      <div class="atlas-method-heading">
        <p class="atlas-eyebrow">Inspiration</p>
        <h2>What if movies had microgenres like music?</h2>
      </div>
      <div class="atlas-method-copy">
        <p>
          The idea started with the pleasure of Spotify microgenres: those oddly specific labels that make taste feel less like a dropdown menu and more like a living cultural map.
        </p>
        <p>
          Movies already have genres, but genre is blunt. A film can be comedy, horror, romance, or sci-fi and still belong to a stranger experiential family: lonely future intimacy, corporate satire, cozy mystery, survival dread, mythic spectacle, suburban paranoia, or slacker noir.
        </p>
        <p>
          The Film Atlas asks whether cinema can be explored the way people often discover music: by texture, mood, story shape, and felt similarity. It is not a replacement for genre. It is a second map underneath it.
        </p>
      </div>
    </div>
  </section>

  <section id="atlas-writeup" class="atlas-writeup-section">
    <div class="atlas-writeup-inner">
      <div class="atlas-writeup-heading">
        <p class="atlas-eyebrow">How I built it</p>
        <h2>From movie metadata to semantic cartography.</h2>
        <p>
          The finished map looks simple: dots, territories, labels, search, and nearest neighbors. The hard part was making those pieces mean the same thing.
        </p>
        <p>
          This project became less about drawing a pretty embedding and more about building a trustworthy semantic geography: a system where the data model, labels, borders, and user interface all describe the same underlying structure.
        </p>
      </div>

      <div class="atlas-writeup-grid">
        <article class="atlas-writeup-card">
          <p class="atlas-writeup-step">01</p>
          <h3>Start with a defensible data story</h3>
          <p>
            The first version used the official TMDb API to build controlled movie profiles from titles, overviews, genres, keywords, and short review-language snippets.
          </p>
          <p>
            That made the project publishable: no Letterboxd scraping, no IMDb review scraping, no private datasets, and no raw review text exposed in the browser. But the early profiles were too thin. They produced some good neighbors, but not enough depth for the kind of vibe map the interface wanted.
          </p>
        </article>

        <article class="atlas-writeup-card">
          <p class="atlas-writeup-step">02</p>
          <h3>Build semantic profiles, not keyword soup</h3>
          <p>
            The stronger version combined multiple public signals: TMDb metadata, MovieLens Tag Genome relevance scores, MPST plot tags and synopses, controlled synopsis texture, and light review language.
          </p>
          <p>
            The key lesson was that more text was not automatically better. Heavy review text added noise. Title-heavy profiles created leakage. The best representation balanced plot, theme, tone, audience perception, and genre without letting any single source dominate.
          </p>
          <p>
            Each film became a semantic profile: a compact description of what the movie is about, how it feels, and what viewers tend to associate with it.
          </p>
        </article>

        <article class="atlas-writeup-card">
          <p class="atlas-writeup-step">03</p>
          <h3>Keep similarity in the full embedding space</h3>
          <p>
            Each semantic profile was embedded into a high-dimensional vector. Nearest neighbors are calculated there, where the semantic signal is strongest. The visible map is a projection. It is useful for exploration, but it is not treated as the source of truth.
          </p>
          <p>
            That distinction mattered. A raw 2D projection can make good semantic neighbors appear far apart, or make nearby dots look more related than they really are. The atlas needed to be visually readable without pretending that screen distance explains everything.
          </p>
        </article>

        <article class="atlas-writeup-card">
          <p class="atlas-writeup-step">04</p>
          <h3>Make the hierarchy real</h3>
          <p>
            The biggest early failure was structural. Macro regions, neighborhoods, and micro clusters originally behaved like separate layers that looked nested in the UI but were not always truly nested in the data.
          </p>
          <p>
            The final system uses strict hierarchical clustering: cluster the full set into macro regions, cluster within each macro region into neighborhoods, then cluster within each neighborhood into micro clusters. That means every film has one real path through the atlas: macro region -> neighborhood -> micro cluster.
          </p>
          <p>
            The geography is not just a visual effect. It is enforced by the data model.
          </p>
        </article>

        <article class="atlas-writeup-card">
          <p class="atlas-writeup-step">05</p>
          <h3>Make labels poetic but accountable</h3>
          <p>
            The labels are the product surface. They are how people understand the map.
          </p>
          <p>
            I used LLM-assisted labeling to generate names for regions, neighborhoods, and micro clusters, but the goal was not to blindly accept whatever sounded clever. Labels had to be vivid enough to feel human and specific enough to be useful.
          </p>
          <p>
            The guiding phrase became: poetic but accountable. A good label should be memorable, but it should also be supported by representative films, tags, cluster evidence, and nearest-neighbor behavior.
          </p>
        </article>

        <article class="atlas-writeup-card">
          <p class="atlas-writeup-step">06</p>
          <h3>Turn the frontend into a QA surface</h3>
          <p>
            The interface exposed problems the pipeline alone could hide. Some labels overpromised. Some borders implied relationships that were not true. Some nearest neighbors were semantically good but visually surprising. Some map experiments looked beautiful but were less honest.
          </p>
          <p>
            Search, selected-film panels, expandable cluster cards, and nearest-neighbor links became more than UI features. They became audit tools.
          </p>
          <p>
            The frontend became a cartography problem: how do you show high-dimensional semantic relationships in a way that feels readable, stable, and honest?
          </p>
        </article>
      </div>

      <div class="atlas-writeup-split atlas-technical-section">
        <div>
          <p class="atlas-eyebrow">Technical implementation</p>
          <h3>The public atlas is static. The semantic system is built offline.</h3>
        </div>
        <div class="atlas-writeup-copy">
          <p>
            The production page is intentionally simple: Astro serves sanitized JSON, and a custom Canvas renderer draws the atlas in the browser. There is no recommendation API, no model call, and no private data dependency on the public page. All of the expensive work happens ahead of time in an offline pipeline.
          </p>
          <p>
            TMDb is the foundation for movie identity and metadata: titles, release years, overviews, genres, keywords, taglines, popularity signals, and stable IDs for matching. The MovieLens Tag Genome adds structured audience-perception signal through tag relevance scores, which helps the atlas capture vibe, tone, theme, and style more directly than genre fields alone. MPST contributes plot tags and controlled synopsis texture, giving thin TMDb summaries more narrative shape without letting long synopses overwhelm the representation.
          </p>
          <p>
            Those sources are combined into semantic profiles: compact, source-balanced descriptions of what each film is about, how it feels, and what viewers tend to associate with it. The profiles are embedded with OpenAI embeddings, then used in two separate ways. Nearest neighbors are computed in the full embedding space, where semantic similarity is strongest. Clustering is also performed in embedding space, using a strict hierarchy: macro regions first, neighborhoods inside each macro region, and micro clusters inside each neighborhood.
          </p>
          <p>
            Labels are generated with LLM assistance, but treated as a reviewable product layer rather than ground truth. The labeling pass uses cluster evidence, representative films, tags, and neighbor behavior to make names vivid enough for a public-facing atlas while keeping them inspectable. The final export keeps only what the browser needs: movie display fields, points, hierarchy records, labels, territory layouts, and neighbor shards. API keys, raw reviews, raw embeddings, and private experiment artifacts stay out of the static site.
          </p>
        </div>
      </div>

      <div class="atlas-experiment-block">
        <div class="atlas-experiment-heading">
          <p class="atlas-eyebrow">Experiments and tradeoffs</p>
          <h3>What improved the atlas, and what did not.</h3>
        </div>
        <div class="atlas-experiment-table-wrap">
          <table class="atlas-experiment-table">
            <thead>
              <tr>
                <th scope="col">Area</th>
                <th scope="col">Tradeoff</th>
                <th scope="col">Final choice</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <th scope="row">Data</th>
                <td>Richer sources improved semantic texture, but scraped review ecosystems would weaken the public data story.</td>
                <td>Use TMDb plus public MovieLens and MPST signals; avoid Letterboxd and IMDb scraping.</td>
              </tr>
              <tr>
                <th scope="row">Profiles</th>
                <td>More text was not automatically better; title-heavy or review-heavy profiles could create leakage and noise.</td>
                <td>Build balanced semantic profiles from plot, theme, tone, tags, and controlled review/synopsis texture.</td>
              </tr>
              <tr>
                <th scope="row">Clustering</th>
                <td>Flat clusters were easier to generate, but made the UI imply a hierarchy the data did not fully support.</td>
                <td>Use a strict macro region -> neighborhood -> micro cluster hierarchy so each film has one real atlas path.</td>
              </tr>
              <tr>
                <th scope="row">Methods</th>
                <td>A beautiful 2D projection can hide or distort high-dimensional relationships.</td>
                <td>Compute neighbors and clusters in full embedding space; use the map as an explorable projection.</td>
              </tr>
              <tr>
                <th scope="row">Labels</th>
                <td>Purely literal labels felt flat; purely poetic labels could overstate the evidence.</td>
                <td>Use LLM-assisted labels that are vivid, inspectable, and accountable to cluster evidence.</td>
              </tr>
              <tr>
                <th scope="row">Validation</th>
                <td>Semantic quality is partly subjective, so a single metric would be false confidence.</td>
                <td>Combine structural scans, spot checks, large audits, failure-set rechecks, and browser QA.</td>
              </tr>
              <tr>
                <th scope="row">UI</th>
                <td>More explanation in the map surface added cognitive weight.</td>
                <td>Keep the interaction lightweight and move deeper methodology lower on the page.</td>
              </tr>
              <tr>
                <th scope="row">Frontend</th>
                <td>A backend could make exploration more dynamic, but would add operational and privacy surface area.</td>
                <td>Ship a static Astro + Canvas experience backed by sanitized JSON and lazy neighbor shards.</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div class="atlas-writeup-split">
        <div>
          <p class="atlas-eyebrow">Validation</p>
          <h3>Subjective quality became measurable enough to improve.</h3>
        </div>
        <div class="atlas-writeup-copy">
          <p>
            A semantic map is partly subjective, so validation could not be purely mathematical. I used a mix of known-film spot checks, cluster-label review, structural scans, large LLM-assisted audits, and targeted repairs.
          </p>
          <p>
            The technical checks focused on whether the hierarchy was real, not merely visual: every movie needed one valid macro region, one neighborhood inside that macro region, and one micro cluster inside that neighborhood. The export was also scanned for duplicate path labels, missing cluster assignments, malformed neighbor lists, and accidental leakage of private artifacts.
          </p>
          <p>
            Nearest-neighbor quality was checked separately from screen position. That mattered because the map is a projection: a point can look distant on the canvas while still being close in the full embedding space. Validation therefore compared selected films against their semantic neighbors, cluster memberships, labels, and visible territory placement as separate signals.
          </p>
          <p>
            Browser QA became part of the validation loop too. Search, zoom, selected-film panels, expandable cluster cards, labels, territory boundaries, and console health were all inspected against the sanitized static export so the public page stayed aligned with the offline pipeline.
          </p>
          <p>
            The final public export contains 10,000 films, 946 labels, 16 macro regions, 180 neighborhoods, and 750 micro clusters. The hierarchy scan found no duplicate macro region/neighborhood/micro cluster path labels, and the browser export contains no API keys, raw reviews, or embeddings.
          </p>
          <p>
            The largest audit before targeted repair scored 9,633 pass, 336 mixed, and 31 fail. After repair rules were applied to the failure set, the recheck found 22 pass, 9 mixed, and 0 fail. I treated audit results as directional quality checks, not as objective truth: evidence that the atlas is useful and inspectable, not proof of perfect classification.
          </p>
        </div>
      </div>

      <div class="atlas-lessons-grid">
        <div class="atlas-lesson">
          <h3>What changed</h3>
          <p>
            The project moved from "more data will fix it" to "the structure has to be true." It moved from flat cluster labels to telescoping region names, from a raw scatterplot to a semantic atlas, and from one-off model output to an audited public product.
          </p>
        </div>
        <div class="atlas-lesson">
          <h3>What worked</h3>
          <p>
            Official and public data sources, balanced semantic profiles, full-space nearest-neighbor computation, strict hierarchy, human-reviewable labels, targeted repairs, and browser QA made the system legible without requiring a backend.
          </p>
        </div>
        <div class="atlas-lesson">
          <h3>What did not</h3>
          <p>
            Heavy review text, title-heavy embeddings, non-nested clustering, purely quantitative cluster selection, and decorative map borders all created attractive but less trustworthy versions of the atlas.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="atlas-credit-section">
    <div>
      <p>
        Movie metadata is sourced from TMDB. This product uses the TMDB API but is not endorsed or certified by TMDB.
      </p>
    </div>
  </section>
</div>

<script>
  import { initFilmAtlas } from "../../lib/film-atlas/map";

  for (const root of document.querySelectorAll<HTMLElement>("[data-film-atlas-root]")) {
    initFilmAtlas(root);
  }
</script>

<style>
  :global(body:has(.film-atlas-page)) {
    --bg: 30 12% 3%;
    --fg: 42 36% 93%;
    --muted: 41 17% 66%;
    --surface: 30 10% 6%;
    --border: 36 28% 18%;
    --accent: 38 82% 69%;
    background: #050505;
  }

  :global(html:has(.film-atlas-page)) {
    background: #050505;
  }

  .film-atlas-page {
    font-feature-settings: "kern";
  }

  .atlas-shell {
    min-height: calc(100vh - 3.5rem);
  }

  .atlas-page-field {
    background:
      linear-gradient(180deg, rgba(5, 5, 5, 0.10), rgba(5, 5, 5, 0.88)),
      linear-gradient(110deg, rgba(104, 216, 197, 0.10), transparent 36%),
      linear-gradient(250deg, rgba(242, 196, 109, 0.11), transparent 42%),
      repeating-linear-gradient(90deg, rgba(242, 196, 109, 0.028) 0 1px, transparent 1px 112px),
      repeating-linear-gradient(0deg, rgba(104, 216, 197, 0.020) 0 1px, transparent 1px 112px),
      #050505;
  }

  .atlas-page-field::after {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(125deg, rgba(242, 196, 109, 0.10), transparent 32%),
      linear-gradient(238deg, rgba(104, 216, 197, 0.09), transparent 38%),
      linear-gradient(180deg, transparent, rgba(5, 5, 5, 0.64));
    content: "";
    opacity: 0.78;
  }

  .atlas-app-frame {
    min-height: calc(100vh - 3.5rem);
    padding: clamp(1.1rem, 2vw, 2rem);
  }

  .atlas-site-nav {
    align-items: center;
    display: flex;
    gap: 1rem;
    justify-content: space-between;
    margin-bottom: clamp(1rem, 2vw, 1.55rem);
    min-height: 2rem;
  }

  .atlas-site-home {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 1.05rem;
    font-weight: 600;
    line-height: 1;
    text-decoration: none;
    transition: color 160ms ease;
  }

  .atlas-site-links {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: clamp(0.8rem, 2vw, 1.25rem);
    justify-content: flex-end;
  }

  .atlas-site-links a {
    color: rgba(217, 199, 163, 0.72);
    font-size: 0.82rem;
    line-height: 1;
    text-decoration: none;
    transition: color 160ms ease;
  }

  .atlas-site-home:hover,
  .atlas-site-home:focus-visible,
  .atlas-site-links a:hover,
  .atlas-site-links a:focus-visible {
    color: #f2c46d;
    outline: none;
  }

  .atlas-masthead {
    align-items: end;
    display: grid;
    gap: 1rem;
    grid-template-columns: minmax(0, 1fr) minmax(330px, 0.34fr);
    margin-bottom: 1rem;
  }

  .atlas-title-block {
    max-width: 58rem;
  }

  .atlas-eyebrow,
  .atlas-panel-eyebrow,
  .atlas-stat-grid dt,
  .atlas-neighbor-section h3 {
    color: #68d8c5;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.68rem;
    letter-spacing: 0;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .atlas-title-block h1 {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 4.65rem;
    font-weight: 600;
    line-height: 0.92;
    margin: 0.42rem 0 0;
  }

  .atlas-dek {
    color: rgba(217, 199, 163, 0.86);
    font-size: 1.08rem;
    line-height: 1.55;
    margin: 0.78rem 0 0;
    max-width: 46rem;
  }

  .atlas-stat-grid {
    display: grid;
    gap: 0.55rem;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin: 0;
  }

  .atlas-stat-grid div {
    background: rgba(5, 5, 5, 0.42);
    border: 1px solid rgba(242, 196, 109, 0.16);
    border-radius: 8px;
    min-width: 0;
    padding: 0.76rem 0.72rem;
  }

  .atlas-stat-grid dd {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 1.28rem;
    line-height: 1.1;
    margin: 0.18rem 0 0;
    white-space: nowrap;
  }

  .atlas-workspace {
    --atlas-panel-height: calc(100vh - 8.2rem + 2px);
    align-items: start;
    display: grid;
    flex: 1;
    gap: 1rem;
    grid-template-columns: minmax(0, 1fr) minmax(330px, 0.34fr);
    min-height: 0;
  }

  .atlas-map-panel,
  .atlas-detail-panel {
    background: rgba(7, 7, 6, 0.88);
    border: 1px solid rgba(242, 196, 109, 0.18);
    border-radius: 8px;
    box-shadow: 0 24px 90px rgba(0, 0, 0, 0.42);
    min-width: 0;
    overflow: hidden;
  }

  .atlas-map-panel {
    display: grid;
    grid-template-rows: auto minmax(28rem, 1fr);
  }

  .atlas-toolbar {
    align-items: center;
    background: rgba(8, 8, 7, 0.84);
    border-bottom: 1px solid rgba(242, 196, 109, 0.12);
    display: grid;
    gap: 0.72rem;
    grid-template-columns: minmax(20rem, 1fr) auto;
    padding: 0.72rem;
  }

  .atlas-search-form {
    min-width: 0;
    position: relative;
  }

  .atlas-search-input {
    background: rgba(13, 11, 9, 0.92);
    border: 1px solid rgba(242, 196, 109, 0.22);
    border-radius: 6px;
    color: #fff8ea;
    font-family: Inter, ui-sans-serif, system-ui;
    font-size: 0.9rem;
    height: 2.7rem;
    outline: none;
    padding: 0 0.95rem;
    transition: border-color 160ms ease, box-shadow 160ms ease, background 160ms ease;
    width: 100%;
  }

  .atlas-search-input::placeholder {
    color: rgba(217, 199, 163, 0.48);
  }

  .atlas-search-input:focus {
    background: rgba(16, 13, 11, 0.98);
    border-color: rgba(242, 196, 109, 0.58);
    box-shadow: 0 0 0 3px rgba(242, 196, 109, 0.10);
  }

  .atlas-control-rack {
    align-items: center;
    display: flex;
    gap: 0.5rem;
    justify-content: flex-end;
    min-width: 0;
  }

  .atlas-tier-pill {
    background: rgba(7, 18, 17, 0.88);
    border: 1px solid rgba(104, 216, 197, 0.24);
    border-radius: 6px;
    color: #8ee6d9;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.66rem;
    line-height: 1;
    padding: 0.76rem 0.78rem;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .atlas-zoom-controls {
    display: inline-flex;
    gap: 0.32rem;
  }

  .atlas-selected-map-chip {
    align-items: center;
    background: rgba(17, 14, 10, 0.94);
    border: 1px solid rgba(242, 196, 109, 0.42);
    border-radius: 6px;
    color: #fff8ea;
    display: inline-flex;
    font-family: Inter, ui-sans-serif, system-ui;
    font-size: 0.76rem;
    gap: 0.5rem;
    line-height: 1;
    max-width: min(20rem, calc(100% - 1.5rem));
    min-height: 2.25rem;
    min-width: 0;
    padding: 0.62rem 0.72rem;
    position: absolute;
    right: 0.75rem;
    top: 0.75rem;
    white-space: nowrap;
    z-index: 5;
  }

  .atlas-selected-map-chip:hover,
  .atlas-selected-map-chip:focus-visible {
    background: rgba(31, 23, 13, 0.98);
    border-color: rgba(242, 196, 109, 0.74);
    outline: none;
  }

  .atlas-selected-map-chip[hidden] {
    display: none;
  }

  .atlas-selected-map-chip span:first-child {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .atlas-selected-map-chip-action {
    color: #f2c46d;
    flex: 0 0 auto;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.64rem;
    text-transform: uppercase;
  }

  .atlas-stage {
    background: #030303;
    min-height: clamp(31rem, calc(100vh - 17.5rem), 48rem);
    overflow: hidden;
    position: relative;
    touch-action: none;
    user-select: none;
  }

  .atlas-stage::after {
    position: absolute;
    inset: 0;
    border: 1px solid rgba(242, 196, 109, 0.08);
    box-shadow: inset 0 0 80px rgba(0, 0, 0, 0.64);
    content: "";
    pointer-events: none;
  }

  :global([data-atlas-status][hidden]) {
    display: none !important;
  }

  .atlas-loading {
    align-items: center;
    background: rgba(5, 5, 5, 0.78);
    color: #f2c46d;
    display: flex;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.72rem;
    inset: 0;
    justify-content: center;
    pointer-events: none;
    position: absolute;
    text-transform: uppercase;
  }

  .atlas-detail-panel {
    align-self: stretch;
  }

  .atlas-detail-scroll {
    height: 100%;
    overflow: auto;
    padding: 1.25rem;
  }

  .atlas-detail-panel h2 {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 2.55rem;
    font-weight: 600;
    line-height: 1.02;
    margin: 0.62rem 0 0;
    overflow-wrap: anywhere;
  }

  .atlas-selected-meta {
    color: rgba(217, 199, 163, 0.72);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.74rem;
    line-height: 1.45;
    margin: 0.56rem 0 0;
    text-transform: uppercase;
  }

  .atlas-genre-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.42rem;
    margin-top: 1.15rem;
  }

  .atlas-selected-overview {
    color: #d9c7a3;
    font-size: 0.94rem;
    line-height: 1.75;
    margin: 1.2rem 0 0;
  }

  .atlas-selected-labels {
    display: grid;
    gap: 0.55rem;
    margin-top: 1.25rem;
  }

  .atlas-neighbor-section {
    border-top: 1px solid rgba(242, 196, 109, 0.14);
    margin-top: 1.7rem;
    padding-top: 1.25rem;
  }

  .atlas-neighbor-section h3 {
    color: #f2c46d;
    margin: 0;
  }

  .atlas-neighbor-list {
    display: grid;
    gap: 0.5rem;
    margin-top: 0.72rem;
  }

  .atlas-icon-button,
  .atlas-reset-button {
    align-items: center;
    background: rgba(13, 11, 9, 0.9);
    border: 1px solid rgba(242, 196, 109, 0.2);
    border-radius: 6px;
    color: #fff8ea;
    display: inline-flex;
    font-family: Inter, ui-sans-serif, system-ui;
    height: 2.25rem;
    justify-content: center;
    transition: border-color 160ms ease, background 160ms ease, color 160ms ease;
  }

  .atlas-icon-button {
    font-size: 1.05rem;
    width: 2.25rem;
  }

  .atlas-reset-button {
    font-size: 0.78rem;
    padding: 0 0.72rem;
  }

  .atlas-icon-button:hover,
  .atlas-reset-button:hover {
    background: rgba(242, 196, 109, 0.1);
    border-color: rgba(242, 196, 109, 0.48);
    color: #f2c46d;
  }

  :global(.atlas-tooltip) {
    background: rgba(7, 7, 7, 0.92);
    border: 1px solid rgba(242, 196, 109, 0.28);
    border-radius: 6px;
    box-shadow: 0 14px 40px rgba(0, 0, 0, 0.44);
    color: #fff8ea;
    left: 0;
    max-width: min(280px, calc(100% - 20px));
    padding: 0.7rem 0.82rem;
    pointer-events: none;
    position: absolute;
    top: 0;
    z-index: 10;
  }

  :global(.atlas-tooltip-title) {
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 1rem;
    line-height: 1.15;
  }

  :global(.atlas-tooltip-meta) {
    color: rgba(217, 199, 163, 0.78);
    font-family: Inter, ui-sans-serif, system-ui;
    font-size: 0.73rem;
    line-height: 1.35;
    margin-top: 0.28rem;
  }

  .atlas-search-results {
    background: rgba(7, 7, 7, 0.98);
    border: 1px solid rgba(242, 196, 109, 0.22);
    border-radius: 6px;
    box-shadow: 0 18px 50px rgba(0, 0, 0, 0.42);
    left: 0;
    max-height: 22rem;
    overflow: auto;
    padding: 0.35rem;
    position: absolute;
    right: 0;
    top: calc(100% + 0.45rem);
    z-index: 20;
  }

  :global(.atlas-search-result),
  :global(.atlas-neighbor-button) {
    background: transparent;
    border: 1px solid transparent;
    border-radius: 5px;
    color: #fff8ea;
    display: grid;
    gap: 0.16rem;
    padding: 0.65rem 0.7rem;
    text-align: left;
    transition: background 160ms ease, border-color 160ms ease;
    width: 100%;
  }

  :global(.atlas-search-result:hover),
  :global(.atlas-search-result[aria-selected="true"]),
  :global(.atlas-neighbor-button:hover) {
    background: rgba(242, 196, 109, 0.08);
    border-color: rgba(242, 196, 109, 0.18);
  }

  :global(.atlas-search-title),
  :global(.atlas-neighbor-title) {
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 1rem;
    line-height: 1.2;
  }

  :global(.atlas-search-meta),
  :global(.atlas-neighbor-score),
  :global(.atlas-search-empty) {
    color: rgba(217, 199, 163, 0.7);
    font-family: Inter, ui-sans-serif, system-ui;
    font-size: 0.73rem;
    line-height: 1.3;
  }

  :global(.atlas-search-empty) {
    padding: 0.75rem;
  }

  :global(.atlas-neighbor-button) {
    align-items: center;
    grid-template-columns: 1fr auto;
  }

  :global(.atlas-label-pill) {
    align-items: flex-start;
    background: rgba(255, 248, 234, 0.035);
    border: 1px solid rgba(242, 196, 109, 0.14);
    border-radius: 6px;
    color: #f7ead2;
    display: grid;
    gap: 0.2rem;
    padding: 0.62rem 0.72rem;
  }

  :global(.atlas-label-disclosure) {
    background: rgba(255, 248, 234, 0.035);
    border: 1px solid rgba(242, 196, 109, 0.14);
    border-radius: 6px;
    color: #f7ead2;
    overflow: hidden;
  }

  :global(.atlas-label-disclosure[open]) {
    border-color: rgba(104, 216, 197, 0.28);
    background: rgba(104, 216, 197, 0.045);
  }

  :global(.atlas-label-summary) {
    align-items: center;
    cursor: pointer;
    display: flex;
    gap: 0.75rem;
    justify-content: space-between;
    list-style: none;
    padding: 0.62rem 0.72rem;
  }

  :global(.atlas-label-summary::-webkit-details-marker) {
    display: none;
  }

  :global(.atlas-label-summary-text) {
    display: grid;
    gap: 0.2rem;
    min-width: 0;
  }

  :global(.atlas-label-value) {
    color: #f7ead2;
    line-height: 1.25;
  }

  :global(.atlas-label-count) {
    color: rgba(217, 199, 163, 0.66);
    font-family: Inter, ui-sans-serif, system-ui;
    font-size: 0.72rem;
    line-height: 1.2;
  }

  :global(.atlas-label-chevron) {
    border-bottom: 2px solid rgba(104, 216, 197, 0.82);
    border-right: 2px solid rgba(104, 216, 197, 0.82);
    flex: 0 0 auto;
    height: 0.46rem;
    transform: rotate(45deg);
    transition: transform 160ms ease, border-color 160ms ease;
    width: 0.46rem;
  }

  :global(.atlas-label-disclosure[open] .atlas-label-chevron) {
    border-color: rgba(242, 196, 109, 0.88);
    transform: rotate(225deg);
  }

  :global(.atlas-label-key) {
    color: #68d8c5;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.64rem;
    text-transform: uppercase;
  }

  :global(.atlas-cluster-members) {
    border-top: 1px solid rgba(242, 196, 109, 0.12);
    display: grid;
    max-height: 18rem;
    overflow: auto;
    padding: 0.35rem;
  }

  :global(.atlas-cluster-member) {
    background: transparent;
    border: 1px solid transparent;
    border-radius: 5px;
    color: #fff8ea;
    display: grid;
    gap: 0.14rem;
    padding: 0.5rem 0.55rem;
    text-align: left;
    transition: background 160ms ease, border-color 160ms ease;
    width: 100%;
  }

  :global(.atlas-cluster-member:hover),
  :global(.atlas-cluster-member[aria-current="true"]) {
    background: rgba(242, 196, 109, 0.08);
    border-color: rgba(242, 196, 109, 0.18);
  }

  :global(.atlas-cluster-member-title) {
    font-size: 0.86rem;
    line-height: 1.2;
  }

  :global(.atlas-cluster-member-meta) {
    color: rgba(217, 199, 163, 0.64);
    font-size: 0.68rem;
    line-height: 1.25;
  }

  :global(.atlas-genre-pill) {
    border: 1px solid rgba(104, 216, 197, 0.22);
    border-radius: 4px;
    color: #9ee7dc;
    font-size: 0.72rem;
    padding: 0.16rem 0.42rem;
  }

  .atlas-method-section {
    background: linear-gradient(180deg, #080706, #050505);
    border-bottom: 1px solid rgba(242, 196, 109, 0.12);
    padding: clamp(3rem, 6vw, 5.5rem) clamp(1rem, 3vw, 2rem);
  }

  .atlas-method-section--inspiration {
    background: #050505;
  }

  .atlas-method-grid {
    display: grid;
    gap: clamp(2rem, 4vw, 4rem);
    grid-template-columns: minmax(18rem, 0.78fr) minmax(0, 1fr);
    margin: 0 auto;
    max-width: 82rem;
  }

  .atlas-method-heading h2 {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 3.4rem;
    line-height: 1;
    margin: 0.7rem 0 0;
  }

  .atlas-method-copy {
    color: rgba(217, 199, 163, 0.86);
    display: grid;
    font-size: 1rem;
    gap: 1.1rem;
    line-height: 1.85;
  }

  .atlas-method-copy p {
    margin: 0;
  }

  .atlas-writeup-section {
    background: #080706;
    border-bottom: 1px solid rgba(242, 196, 109, 0.12);
    padding: clamp(3.5rem, 7vw, 6.5rem) clamp(1rem, 3vw, 2rem);
  }

  .atlas-writeup-inner {
    margin: 0 auto;
    max-width: 82rem;
  }

  .atlas-writeup-heading {
    max-width: 58rem;
  }

  .atlas-writeup-heading h2 {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: clamp(2.65rem, 5vw, 4.6rem);
    font-weight: 600;
    line-height: 0.98;
    margin: 0.72rem 0 0;
  }

  .atlas-writeup-heading p:not(.atlas-eyebrow) {
    color: rgba(217, 199, 163, 0.86);
    font-size: 1.02rem;
    line-height: 1.82;
    margin: 1.2rem 0 0;
    max-width: 48rem;
  }

  .atlas-writeup-grid {
    display: grid;
    gap: 0.85rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: clamp(2rem, 4vw, 3.5rem);
  }

  .atlas-writeup-card,
  .atlas-lesson {
    background: rgba(5, 5, 5, 0.42);
    border: 1px solid rgba(242, 196, 109, 0.15);
    border-radius: 8px;
    padding: 1rem;
  }

  .atlas-writeup-step {
    color: #68d8c5;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.68rem;
    line-height: 1;
    margin: 0;
  }

  .atlas-writeup-card h3,
  .atlas-writeup-split h3,
  .atlas-lesson h3 {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 1.35rem;
    font-weight: 600;
    line-height: 1.08;
    margin: 0.72rem 0 0;
  }

  .atlas-writeup-card p:not(.atlas-writeup-step),
  .atlas-writeup-copy,
  .atlas-lesson p {
    color: rgba(217, 199, 163, 0.84);
    font-size: 0.92rem;
    line-height: 1.75;
  }

  .atlas-writeup-card p:not(.atlas-writeup-step),
  .atlas-lesson p {
    margin: 0.78rem 0 0;
  }

  .atlas-writeup-split {
    border-top: 1px solid rgba(242, 196, 109, 0.14);
    display: grid;
    gap: clamp(1.5rem, 3vw, 3rem);
    grid-template-columns: minmax(18rem, 0.78fr) minmax(0, 1fr);
    margin-top: clamp(2.5rem, 5vw, 4rem);
    padding-top: clamp(2rem, 4vw, 3rem);
  }

  .atlas-technical-section {
    margin-top: clamp(2.5rem, 5vw, 4rem);
  }

  .atlas-writeup-copy {
    display: grid;
    gap: 1rem;
  }

  .atlas-writeup-copy p {
    margin: 0;
  }

  .atlas-experiment-block {
    border-top: 1px solid rgba(242, 196, 109, 0.14);
    display: grid;
    gap: clamp(1.25rem, 2.5vw, 2rem);
    margin-top: clamp(2.5rem, 5vw, 4rem);
    padding-top: clamp(2rem, 4vw, 3rem);
  }

  .atlas-experiment-heading {
    max-width: 42rem;
  }

  .atlas-experiment-heading h3 {
    color: #fff8ea;
    font-family: "Source Serif 4", ui-serif, Georgia, serif;
    font-size: 1.6rem;
    font-weight: 600;
    line-height: 1.08;
    margin: 0.72rem 0 0;
  }

  .atlas-experiment-table-wrap {
    overflow-x: auto;
  }

  .atlas-experiment-table {
    border-collapse: collapse;
    color: rgba(217, 199, 163, 0.84);
    font-size: 0.88rem;
    line-height: 1.55;
    min-width: 48rem;
    width: 100%;
  }

  .atlas-experiment-table th,
  .atlas-experiment-table td {
    border-bottom: 1px solid rgba(242, 196, 109, 0.12);
    padding: 0.82rem 0.9rem;
    text-align: left;
    vertical-align: top;
  }

  .atlas-experiment-table thead th {
    color: #68d8c5;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.66rem;
    text-transform: uppercase;
  }

  .atlas-experiment-table tbody th {
    color: #fff8ea;
    font-weight: 600;
    white-space: nowrap;
  }

  .atlas-lessons-grid {
    display: grid;
    gap: 0.85rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: clamp(1.5rem, 3vw, 2.25rem);
  }

  .atlas-credit-section {
    background: #050505;
    color: rgba(217, 199, 163, 0.78);
    font-size: 0.86rem;
    line-height: 1.7;
    padding: 1.8rem clamp(1rem, 3vw, 2rem);
  }

  .atlas-credit-section div {
    margin: 0 auto;
    max-width: 82rem;
  }

  .atlas-credit-section p {
    margin: 0;
  }

  @media (min-width: 1024px) {
    .atlas-map-panel,
    .atlas-detail-panel {
      height: var(--atlas-panel-height);
    }

    .atlas-detail-panel {
      position: sticky;
      top: 1rem;
    }
  }

  @media (max-width: 1180px) {
    .atlas-title-block h1 {
      font-size: 3.85rem;
    }

    .atlas-detail-panel h2 {
      font-size: 2.35rem;
    }

    .atlas-masthead {
      grid-template-columns: 1fr;
    }

    .atlas-stat-grid {
      max-width: 42rem;
    }

    .atlas-toolbar {
      grid-template-columns: 1fr;
    }

    .atlas-writeup-grid,
    .atlas-lessons-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .atlas-control-rack {
      justify-content: flex-start;
      flex-wrap: wrap;
    }
  }

  @media (max-width: 1023px) {
    .atlas-workspace,
    .atlas-method-grid,
    .atlas-writeup-split {
      grid-template-columns: 1fr;
    }

    .atlas-detail-panel {
      min-height: 22rem;
    }

    .atlas-detail-scroll {
      height: auto;
      max-height: none;
    }
  }

  @media (max-width: 640px) {
    .atlas-app-frame {
      padding: 0.85rem;
    }

    .atlas-title-block h1 {
      font-size: 3rem;
    }

    .atlas-dek {
      font-size: 0.98rem;
    }

    .atlas-detail-panel h2 {
      font-size: 2.1rem;
    }

    .atlas-method-heading h2 {
      font-size: 2.45rem;
    }

    .atlas-writeup-grid,
    .atlas-lessons-grid {
      grid-template-columns: 1fr;
    }

    .atlas-masthead {
      margin-bottom: 0.75rem;
    }

    .atlas-stat-grid {
      grid-template-columns: 1fr;
    }

    .atlas-toolbar {
      padding: 0.58rem;
    }

    .atlas-tier-pill {
      text-align: center;
    }

    .atlas-zoom-controls {
      justify-content: end;
    }

    .atlas-selected-map-chip {
      max-width: calc(100% - 1rem);
      right: 0.5rem;
      top: 0.5rem;
    }

    .atlas-stage {
      min-height: 28rem;
    }

    .atlas-page-field::after {
      opacity: 0.48;
    }
  }
</style>
